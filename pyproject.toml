[project]
name = "card-extractor"
version = "0.1.0"
description = "Extraction de cartes Pokémon depuis des scans de slabs gradés (PSA/PCA)"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.10",
    "numpy>=2.0",
]
